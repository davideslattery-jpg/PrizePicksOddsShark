name: Update edges board

on:
  schedule:
    # Free-tier budget: ~2 refreshes/day during US sports window (UTC)
    - cron: "15 17 * * *"
    - cron: "15 1 * * *"
  workflow_dispatch:

permissions:
  contents: write

concurrency:
  group: update-edges
  cancel-in-progress: true

jobs:
  export-edges:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install package
        run: pip install -e .

      - name: Export edges (live or demo)
        env:
          ODDS_API_KEY: ${{ secrets.ODDS_API_KEY }}
        run: |
          set -euo pipefail
          mkdir -p docs/data
          OUT="docs/data/edges.json"
          # Free-tier mode: NFL + NBA only, few events, lean markets (no alternates)
          SPORTS="basketball_nba,americanfootball_nfl"
          MAX_EVENTS=2
          if [ -n "${ODDS_API_KEY:-}" ]; then
            echo "ODDS_API_KEY present — free-tier live export (--sport ${SPORTS}, lean)"
            if pp-odds --sport "$SPORTS" --lean --min-edge 2 --max-events "$MAX_EVENTS" --export "$OUT"; then
              echo "Live export OK"
            else
              echo "Live export failed — falling back to --demo"
              pp-odds --demo --sport basketball_nba --min-edge 2 --export "$OUT"
            fi
          else
            echo "ODDS_API_KEY missing — publishing demo board"
            pp-odds --demo --sport basketball_nba --min-edge 2 --export "$OUT"
          fi
          python - <<'PY2'
          import json
          from collections import Counter
          from pathlib import Path
          p = Path("docs/data/edges.json")
          data = json.loads(p.read_text(encoding="utf-8"))
          assert "edges" in data and "updated_at" in data
          counts = Counter(e.get("sport") or "?" for e in data.get("edges") or [])
          print(
              f"board mode={data.get('mode')} count={data.get('count')} "
              f"sports={data.get('sports')} per_sport={dict(counts)} "
              f"updated_at={data.get('updated_at')}"
          )
          PY2

      - name: Commit board if changed
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add docs/data/edges.json
          if git diff --staged --quiet; then
            echo "No changes to edges.json — skip commit"
          else
            git commit -m "chore: refresh docs/data/edges.json [skip ci]"
            git push
          fi
